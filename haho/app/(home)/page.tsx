import Navigation from "@/components/navigation";
export const metadata = {
  title: {
    default: "home",
    template: "%s"
  }
}

export default function Home() {
  return (
    <div>
      home!
    </div>
  );
}
